<?php 

$usuario = $_POST['usuario'];
$password = $_POST['password'];

#leer usuario y password de la base de datos, mostrar id si se puede

#Darle la condicion si existe o no 

if (condition) {
    # code...
    #redirige a la pagina principal
    #mantener sesion iniciada para luego cerrar sesion
    #Que aparezca un imagen en forma de circulo, como menu desplegable y que ahi muestre cerrar sesion y su usuario
} else {
    # usuario o contraseña incorrecta
}

header("location:inndex.php")

?>